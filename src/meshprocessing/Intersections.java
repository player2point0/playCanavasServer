package meshprocessing;

public class Intersections 
{

	//sort silhouette points by y (latitude)

	
	
	//have a list of edges that are visible in a given y row, sorted by x

	//in each case it is a bubble sort to insert the next (we could have an index sort with a number of edges in each bin and then a list for when there are extra)
	
	//one for the previous row
	//one for the current row
	
}
